# Vercel 배포 참고

- 푸시 후에도 예전 버전이 보이면:
  1. Vercel → **Deployments** → 맨 위 배포가 방금 푸시한 커밋인지 확인
  2. **Redeploy** 시 **Clear build cache** 옵션 체크 후 재배포
  3. **Settings** → **Git** 에서 Production Branch가 `main` 인지 확인
